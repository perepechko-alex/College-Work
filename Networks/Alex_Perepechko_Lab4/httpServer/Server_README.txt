To run server.py, open up the command-line and type in the following:

python server.py --base=path/to/site_directory --port=####

For example, running 'python server.py --base /usr/home/Desktop/example_site --port 8080' will run index.html on port 8080. Any errors that are raised (404, 501) and all responses are displayed in the terminal.
